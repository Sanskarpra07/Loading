# Personal Portfolio (HTML/CSS/JS)

This is a single-page personal portfolio built with vanilla HTML, CSS and JavaScript. Replace placeholder text, images and icons as instructed.

Quick start:

1. Open `index.html` in your browser (or run a local server).
2. Replace placeholder images in `/assets` with your own. Keep aspect ratios.
3. Update text placeholders marked with `<!-- REPLACE: ... -->` comments.

Files:
- `index.html` — main structure with comments before every section.
- `css/style.css` — all styles. Edit CSS variables at the top to re-theme.
- `js/main.js` — all JavaScript for animations and interactions.
- `assets/` — replace placeholders (profile, thumbs, CV file).

Styling tips:
- Edit colors in `:root` (css/style.css) to change palette.
- Fonts are loaded from Google Fonts: `Space Grotesk` for headings and `Inter` for body.

Accessibility & Performance:
- Semantic HTML and aria-labels included.
- Images use `loading="lazy"` where possible.

Customization checklist:
- Replace any `<!-- REPLACE: ... -->` and example texts.
- Replace SVG/placeholder images in `/assets/`.
- Replace `assets/CV-placeholder.pdf` with your actual CV PDF.

If you want me to push additional features (contact form email integration, build pipeline, or animations refinement), tell me which and I'll implement them.
